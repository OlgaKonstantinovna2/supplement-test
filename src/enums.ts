export enum Platform {
    PC = "PC",
    PlayStation = "PlayStation",
    Xbox = "Xbox",
    Linux = "Linux",
}

export enum Language {
    All = "All",
    Russian = "Russian",
}

export enum SortBy {
    Name = "Name",
    Rating = "Rating",
}